# 📈 Real-time Stock Tracker (Alpaca API)

แอปพลิเคชัน React สำหรับติดตามราคาหุ้นและ ETF ตลาดสหรัฐแบบเรียลไทม์
โดยใช้ข้อมูลจาก **Alpaca API**

---

## 🚀 วิธีรันแอปครั้งแรก (First-time Setup)

1. ติดตั้ง dependencies

```bash
npm install
```

2. สร้างไฟล์ `.env` ที่ root folder ของโปรเจค
   โดยอ้างอิงจากไฟล์ `.env.example`

3. กำหนดค่า Alpaca API Key

```env
VITE_APCA_API_KEY=your_api_key
VITE_APCA_SECRET_KEY=your_secret_key
```

> สามารถขอ API Key ได้จากเว็บไซต์ Alpaca

4. รันแอปในโหมด Development

```bash
npm run dev
```

จากนั้นเปิดเบราว์เซอร์ไปที่ URL ที่ Vite แสดงขึ้นมา (ปกติคือ `http://localhost:5173`)

---

## 🧪 วิธีทดสอบแอป (Run Test Script)

รันทดสอบด้วยคำสั่ง

```bash
npm run test
```

---

## ⚠️ หมายเหตุ

* แอปนี้ใช้ **ข้อมูลราคาจาก wss://stream.data.alpaca.markets/v2/iex**
   โดย handshake ด้วย {"action":"subscribe","bars":["SYMBOL", "etc.."]}
  (ยังไม่แน่ใจว่าการใช้งานจริงควรใช้ `bars` หรือ `trades` ครับ)
* ราคาจะอัปเดต **ทุก ๆ 1 นาที**
* ราคาจะเริ่มขยับและอัปเดตหลังเวลา **21:30 น. (เวลาประเทศไทย)**
  ซึ่งเป็นช่วงที่ตลาดหุ้นสหรัฐเปิดทำการ
